apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: sonarr
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: sonarr
  filterTags:
    pattern: '^[0-9]+\.[0-9]+\.[0-9]+\.(?P<build>[0-9]+)$'
    extract: '$build'
  policy:
    numerical:
      order: asc
