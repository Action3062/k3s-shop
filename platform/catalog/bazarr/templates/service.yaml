apiVersion: v1
kind: Service
metadata:
  name: {{ include "bazarr.fullname" . }}
spec:
  selector:
    app: bazarr
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.port }}
    - name: app
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
