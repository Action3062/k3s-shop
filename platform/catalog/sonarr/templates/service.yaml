apiVersion: v1
kind: Service
metadata:
  name: {{ include "sonarr.fullname" . }}
spec:
  selector:
    app: sonarr
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.port }}
    - name: app
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
