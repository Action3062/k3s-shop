# Pro-Tenant-Werte injiziert die Control-Plane über die HelmRelease.
username: ""
appName: sonarr
baseDomain: meinappnest.org
replicas: 1
image:
  repository: ghcr.io/action3062/sonarr
  tag: "4.0.17.2952"        # Default; Control-Plane pinnt pro Tenant (Update beim Neustart)
  pullPolicy: IfNotPresent
port: 8989
timezone: Europe/Berlin
storage:
  size: 5Gi
  storageClassName: hcloud-volumes
resources:
  requests: { cpu: 100m, memory: 192Mi }
  limits:   { cpu: 750m, memory: 768Mi }
ingress:
  className: traefik
  entrypoint: websecure
  useDefaultWildcardCert: false
  certIssuer: ""
restartToken: ""
dataVersion: 1
