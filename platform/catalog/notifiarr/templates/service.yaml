apiVersion: v1
kind: Service
metadata:
  name: {{ include "notifiarr.fullname" . }}
spec:
  selector:
    app: notifiarr
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.port }}
    - name: app
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
