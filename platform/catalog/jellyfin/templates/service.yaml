apiVersion: v1
kind: Service
metadata:
  name: {{ include "jellyfin.fullname" . }}
spec:
  selector:
    app: jellyfin
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.port }}
    - name: app
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
