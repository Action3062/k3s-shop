apiVersion: v1
kind: Service
metadata:
  name: {{ include "vaultwarden.fullname" . }}
spec:
  selector:
    app: vaultwarden
  ports:
    - port: 80
      targetPort: {{ .Values.port | default 8000 }}
