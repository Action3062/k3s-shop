apiVersion: v1
kind: Service
metadata:
  name: {{ include "homepage.fullname" . }}
spec:
  selector:
    app: homepage
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.port }}
    - name: app
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
