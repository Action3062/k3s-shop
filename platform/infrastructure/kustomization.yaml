apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
# cert-manager (DNS-01 Wildcard) + reflector werden aktiviert, sobald PowerDNS-TSIG vorliegt.
# Siehe cert-manager/ (Manifeste liegen bereit).
