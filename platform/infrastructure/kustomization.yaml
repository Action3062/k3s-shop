apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cert-manager
  - ghcr-pull-secret.sops.yaml   # GHCR-Pull-Secret (SOPS), per Reflector -> tenant-*-vaultwarden
# Cluster-Prereqs (out-of-band installiert, NICHT Flux-verwaltet):
#  - cert-manager
#  - reflector (emberstack) — spiegelt wildcard-vaultwarden-tls in tenant-*-vaultwarden
#  - Secret cloudflare-api-token (ns cert-manager) — KEIN Secret im Git
